my main data is in this location:

INPUT_RAW_DATA_PATH = r"D:\OneDrive\Work\Driver Survey\DataSources\column_rename.xlsx"
INPUT_COLUMN_CHECK_PATH = r"D:\OneDrive\Work\Driver Survey\DataSources\Raw_Data.xlsx"


my raw_data is the data of a survey and it needs to be cleaned before I am going to analyze it. its first row are questions and all the rows below it are its answers

I also have an excel file that I want to use to rename column names of my raw_data: column_rename (sheet=questions)

column_rename(questions) has 2 main rows: first row is the 135 questions in raw_data. second row is the replaced values for questions (raw_data column headers should be replaced by this)
first check if raw_data still has the same headers as column_rename (same questions,same order,same number of questions)
then replace the questions with data from the 2nd row of column_rename
column_rename(sheet questions) also has data in rows>=3. they are questionnaire's unique answers:
if 2nd row pf sheet question(new column names) start with "ignore" or if 3rd row is "customized_answer", then clear all the rows of answers from my raw_data file. otherwise, check the unique answers of raw_data and see if all the options are available in rows>=3 of sheet=questions.
if not, raise an error in console.
if they are the same, then you should replace all the answered value in raw_data with their respective value from sheet replaced_answers of column_rename workbook


sheet replaced_answers is the same as 'questions', but it has English unique answers instead of the persian unique answers in sheet 'questions' (and also, all the answers in raw_data)




______________________________________________________________________


now, if the renamed column 